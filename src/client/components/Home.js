import React from "react";

const Home = () => {
  return (
    <div>
      <div>Homepage</div>
      <button onClick={() => console.log("click me")}>点击我</button>
    </div>
  );
};

export default Home;
